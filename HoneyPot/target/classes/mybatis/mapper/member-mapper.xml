<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
	<select id="mlogin" resultType="com.hp.app.member.vo.MemberVo">
		SELECT * FROM MEMBER
		WHERE ID =
		#{id} AND PWD = #{pwd}
		AND STATUS = 'Y'
	</select>

	<select id="alogin" resultType="com.hp.app.admin.vo.AdminVo">
		SELECT * FROM ADMIN
		WHERE ID =
		#{id} AND PWD = #{pwd}
	</select>

	<insert id="mjoin">
		INSERT INTO MEMBER
		(NO, NAME, ID, PWD, DONG_NUM,
		HO_NUM, BIRTH, PHONE, EMAIL, PROFILE)
		VALUES
		(SEQ_MEMBER_NO.NEXTVAL,
		#{name}, #{id}, #{pwd}, #{dongNum}, #{hoNum},
		#{birth}, #{phone},
		#{email}, #{profile})
	</insert>

	<insert id="ajoin">
		INSERT INTO ADMIN
		(NO, ID, PWD, NAME, GRADE, PROFILE)
		VALUES
		(SEQ_ADMIN_NO.NEXTVAL, #{id}, #{pwd}, #{name}, #{grade},
		#{profile})
	</insert>

	<update id="medit">
		UPDATE MEMBER
		<set>
			Id = #{id},
			PWD = #{pwd},
			PHONE = #{phone},
			EMAIL = #{email}
		</set>
		<where>
			NO = #{no}
		</where>
	</update>

	<update id="aedit">
		UPDATE ADMIN
		<set>
			ID = #{id}
			PWD = #{pwd},
			NAME = #{name}
		</set>
		<where>
			NO = #{no}
		</where>
	</update>

	<select id="idDubCheck" resultType="int">
		SELECT COUNT(*) FROM MEMBER
		WHERE ID = #{id}
	</select>

	<update id="quit">
		UPDATE MEMBER SET STATUS = 'N' 
		WHERE NO = #{no}
	</update>
</mapper>