<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="account">

	
	
	<select id="listCnt" resultType="int">
		SELECT COUNT(NO)
		FROM ACCOUNT A
		WHERE A.WRITER_NO = 5
	</select>
	
	<select id="list" resultMap="com.hp.app.account.vo.AccountVo, com.hp.app.account.vo.AccountCategoryVo">
		SELECT 
		    A.NO
		    ,A.CONTENT
		    ,A.PRICE
		    ,A.ACCOUNT_DATE
            ,AC.NAME
		FROM ACCOUNT A
		JOIN ACCOUNT_CATEGORY AC 
		ON A.ACCOUNT_CNO = AC.NO
		JOIN MEMBER M
		ON A.WRITER_NO = M.NO
		WHERE A.WRITER_NO = 5
		ORDER BY A.ACCOUNT_DATE DESC
	</select>
	

</mapper>